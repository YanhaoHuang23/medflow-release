# Third-party notices

`medflow_release/classifier/model.py` and
`medflow_release/classifier/classifier_train.py` retain their upstream
copyright and MIT licence notice for Microsoft Corporation. They are included
only because the reported EHR configuration optionally trains and uses this
post-hoc selection classifier.

The full text is provided in `MICROSOFT_MIT_LICENSE.txt`.

No TarDiff generator, baseline implementation, baseline checkpoint, or
baseline result is bundled in this release. Dataset licences and access terms
for MIMIC-III, eICU, APAVA, and PTB remain with their respective providers.
